# This file is for documenting secret-based environment variables only (e.g., API keys, passwords)
# Note: Base URLs are defined in code, not here. See config/environments.ts for URL configuration.

# Example: Uncomment and set actual values when needed
# API_KEY=your_api_key_here
# DATABASE_PASSWORD=your_password_here
